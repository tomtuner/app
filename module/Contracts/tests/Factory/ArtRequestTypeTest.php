<?php

/**
 * Art Request Type Test
 *
 * Although the class being tested
 * only has ENUM's we have to make sure
 * the values are the as that in the database
 * @author Nikesh Hajari
 */
class ArtRequestTypeTest
{
    
    public function testArtRequestConstant()
    {
        
        /*$rowCount = \ORMModel\ArtRequestTypeQuery::create()
                        ->filterByArtRequestTypeName(ArtRequest\Factory\ArtRequestType::BANNER_REQUEST)
                        ->count();
        
        $this->assertEquals('1', $rowCount, 'Banner Request Type Not Found');*/
        
        //new \ArtRequest\Factory\ArtRequestType();
       // Stop here and mark this test as incomplete.
        $this->markTestIncomplete(
          'This test has not been implemented yet.'
        );
        
    }
    
}

?>
